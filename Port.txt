<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Preethi S — AI Engineer</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<style>
  :root {
    --accent: #7c3aed;
    --accent-light: #a78bfa;
    --accent-glow: rgba(124,58,237,0.35);
    --bg: #0a0a0f;
    --bg2: #111118;
    --bg3: #16161f;
    --surface: #1c1c28;
    --surface2: #22223a;
    --border: rgba(124,58,237,0.18);
    --text: #e8e8f0;
    --text-muted: #7c7c9a;
    --text-dim: #4a4a6a;
    --font-display: 'Syne', sans-serif;
    --font-body: 'DM Sans', sans-serif;
    --radius: 16px;
    --radius-sm: 8px;
  }
  .theme-teal { --accent: #0d9488; --accent-light: #5eead4; --accent-glow: rgba(13,148,136,0.35); --border: rgba(13,148,136,0.18); }
  .theme-amber { --accent: #d97706; --accent-light: #fbbf24; --accent-glow: rgba(217,119,6,0.35); --border: rgba(217,119,6,0.18); }
  .theme-rose { --accent: #e11d48; --accent-light: #fb7185; --accent-glow: rgba(225,29,72,0.35); --border: rgba(225,29,72,0.18); }
  .theme-indigo { --accent: #4338ca; --accent-light: #818cf8; --accent-glow: rgba(67,56,202,0.35); --border: rgba(67,56,202,0.18); }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body {
    background: var(--bg); color: var(--text); font-family: var(--font-body);
    font-size: 16px; line-height: 1.6; overflow-x: hidden; cursor: none;
  }
  @media (pointer: coarse) { body { cursor: auto; } }

  body::before {
    content: ''; position: fixed; inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none; z-index: 9999; opacity: .45;
  }

  #progress-top { position: fixed; top: 0; left: 0; height: 3px; width: 0%; background: linear-gradient(90deg, var(--accent), var(--accent-light)); z-index: 10000; transition: width .1s linear; }
  #progress-top::after { content: ''; position: absolute; right: -4px; top: -3px; width: 9px; height: 9px; border-radius: 50%; background: var(--accent-light); box-shadow: 0 0 12px var(--accent-light), 0 0 24px var(--accent); }
  #progress-right { position: fixed; right: 0; top: 0; width: 3px; height: 0%; background: linear-gradient(180deg, var(--accent), var(--accent-light)); z-index: 10000; }
  #progress-right::after { content: ''; position: absolute; bottom: -4px; right: -3px; width: 9px; height: 9px; border-radius: 50%; background: var(--accent-light); box-shadow: 0 0 12px var(--accent-light), 0 0 24px var(--accent); }
  @media (max-width: 768px) { #progress-right { display: none; } }

  #cursor-dot { position: fixed; width: 6px; height: 6px; background: var(--accent-light); border-radius: 50%; pointer-events: none; z-index: 99999; transform: translate(-50%,-50%); transition: background .3s; }
  #cursor-ring { position: fixed; width: 32px; height: 32px; border: 1.5px solid var(--accent-light); border-radius: 50%; pointer-events: none; z-index: 99998; transform: translate(-50%,-50%); transition: width .3s, height .3s, border-color .3s, opacity .3s; opacity: .7; }
  #cursor-ring.hover { width: 52px; height: 52px; opacity: 1; border-color: var(--accent); }
  @media (pointer: coarse) { #cursor-dot, #cursor-ring { display: none; } }

  nav { position: fixed; top: 0; left: 0; right: 0; z-index: 1000; padding: 20px 48px; display: flex; align-items: center; justify-content: space-between; backdrop-filter: blur(16px); background: rgba(10,10,15,.7); border-bottom: 1px solid rgba(255,255,255,.04); transition: padding .3s; }
  nav.scrolled { padding: 14px 48px; }
  .nav-logo { font-family: var(--font-display); font-size: 1.25rem; font-weight: 800; color: var(--text); text-decoration: none; letter-spacing: -.02em; position: relative; }
  .nav-logo span { color: var(--accent-light); }
  #theme-label { position: absolute; top: -28px; left: 50%; transform: translateX(-50%); background: var(--surface); color: var(--accent-light); font-family: var(--font-body); font-size: .7rem; font-weight: 500; padding: 2px 8px; border-radius: 4px; white-space: nowrap; opacity: 0; transition: opacity .3s; pointer-events: none; }
  #theme-label.visible { opacity: 1; }
  .nav-links { display: flex; gap: 32px; list-style: none; }
  .nav-links a { color: var(--text-muted); text-decoration: none; font-size: .875rem; font-weight: 500; letter-spacing: .02em; transition: color .2s; position: relative; }
  .nav-links a::after { content: ''; position: absolute; bottom: -4px; left: 0; width: 0; height: 1px; background: var(--accent-light); transition: width .3s; }
  .nav-links a:hover, .nav-links a.active { color: var(--accent-light); }
  .nav-links a:hover::after, .nav-links a.active::after { width: 100%; }
  @media (max-width: 768px) { nav { padding: 16px 20px; } .nav-links { display: none; } nav.scrolled { padding: 12px 20px; } }

  section { position: relative; }
  .container { max-width: 1200px; margin: 0 auto; padding: 0 48px; }
  @media (max-width: 768px) { .container { padding: 0 20px; } }

  #hero { min-height: 100vh; display: flex; align-items: center; position: relative; overflow: hidden; padding-top: 80px; }
  #hero-canvas { position: absolute; inset: 0; z-index: 0; }
  .hero-inner { position: relative; z-index: 1; display: grid; grid-template-columns: 1fr auto; align-items: center; gap: 48px; width: 100%; }
  .hero-eyebrow { font-size: .8rem; font-weight: 500; letter-spacing: .18em; color: var(--accent-light); text-transform: uppercase; margin-bottom: 16px; opacity: 0; transform: translateY(20px); animation: fadeUp .8s .2s forwards; }
  .hero-name { font-family: var(--font-display); font-size: clamp(3.5rem, 10vw, 9rem); font-weight: 800; line-height: .9; letter-spacing: -.04em; color: var(--text); margin-bottom: 24px; opacity: 0; transform: translateY(30px); animation: fadeUp .9s .35s forwards; }
  .hero-name .accent-word { color: var(--accent-light); display: inline-block; }
  .hero-tagline { font-size: clamp(1rem, 2vw, 1.25rem); color: var(--text-muted); font-weight: 300; max-width: 560px; line-height: 1.6; margin-bottom: 40px; opacity: 0; transform: translateY(20px); animation: fadeUp .8s .55s forwards; }
  .hero-ctas { display: flex; gap: 16px; flex-wrap: wrap; opacity: 0; transform: translateY(20px); animation: fadeUp .8s .7s forwards; }
  .btn { display: inline-flex; align-items: center; gap: 8px; padding: 14px 28px; border-radius: 100px; font-weight: 600; font-size: .9rem; text-decoration: none; transition: all .3s; cursor: none; position: relative; overflow: hidden; }
  .btn-primary { background: var(--accent); color: #fff; box-shadow: 0 0 32px var(--accent-glow); }
  .btn-primary:hover { background: var(--accent-light); box-shadow: 0 0 48px var(--accent-glow); transform: translateY(-2px); }
  .btn-secondary { background: transparent; color: var(--text); border: 1px solid var(--border); }
  .btn-secondary:hover { border-color: var(--accent-light); color: var(--accent-light); transform: translateY(-2px); }

  .hero-avatar { width: 140px; height: 140px; border-radius: 50%; background: var(--surface2); border: 2px solid var(--border); display: flex; align-items: center; justify-content: center; font-family: var(--font-display); font-size: 2.5rem; font-weight: 800; color: var(--accent-light); box-shadow: 0 0 0 6px rgba(124,58,237,.08), 0 0 48px var(--accent-glow); flex-shrink: 0; opacity: 0; animation: fadeIn 1s .9s forwards; position: relative; }
  .hero-avatar::after { content: ''; position: absolute; inset: -10px; border-radius: 50%; border: 1px dashed var(--border); animation: spin 12s linear infinite; }
  @keyframes spin { to { transform: rotate(360deg); } }
  @media (max-width: 768px) { .hero-inner { grid-template-columns: 1fr; } .hero-avatar { display: none; } }

  .hero-socials { display: flex; gap: 20px; margin-top: 32px; opacity: 0; transform: translateY(20px); animation: fadeUp .8s .85s forwards; }
  .hero-socials a { display: inline-flex; align-items: center; justify-content: center; width: 40px; height: 40px; border-radius: 50%; color: var(--text-muted); font-size: 1.3rem; transition: color .2s, transform .2s, background .2s; position: relative; z-index: 1001; cursor: pointer; }
  .hero-socials a:hover { color: var(--accent-light); transform: translateY(-3px); background: var(--surface); }

  .marquee-wrap { overflow: hidden; background: var(--bg2); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); padding: 18px 0; margin: 0; }
  .marquee-track { display: flex; gap: 0; white-space: nowrap; animation: marquee 30s linear infinite; }
  .marquee-wrap:hover .marquee-track { animation-play-state: paused; }
  .marquee-item { display: inline-flex; align-items: center; gap: 10px; padding: 0 28px; font-size: .85rem; font-weight: 500; color: var(--text-muted); letter-spacing: .06em; text-transform: uppercase; }
  .marquee-item::before { content: '◆'; color: var(--accent); font-size: .5rem; }
  @keyframes marquee { to { transform: translateX(-50%); } }

  .section-header { margin-bottom: 56px; }
  .section-eyebrow { font-size: .75rem; font-weight: 600; letter-spacing: .2em; color: var(--accent-light); text-transform: uppercase; margin-bottom: 12px; display: block; }
  .section-title { font-family: var(--font-display); font-size: clamp(2rem, 5vw, 3.5rem); font-weight: 800; letter-spacing: -.03em; line-height: 1; color: var(--text); }
  .section-title .dim { color: var(--text-dim); }

  #about { padding: 120px 0; }
  .about-grid { display: grid; grid-template-columns: 2fr 1fr; grid-template-rows: auto auto; gap: 16px; }
  .bento-cell { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 32px; position: relative; overflow: hidden; transition: border-color .3s, transform .15s; }
  .bento-cell::before { content: ''; position: absolute; inset: 0; border-radius: inherit; background: radial-gradient(600px circle at var(--mx,50%) var(--my,50%), rgba(124,58,237,.08), transparent 60%); pointer-events: none; transition: opacity .3s; opacity: 0; }
  .bento-cell:hover::before { opacity: 1; }
  .bento-cell:hover { border-color: var(--accent); }
  .bento-bio { grid-column: 1; grid-row: 1 / 3; }
  .bento-bio p { color: var(--text-muted); line-height: 1.8; font-size: 1.05rem; }
  .bento-bio p + p { margin-top: 16px; }
  .bento-bio strong { color: var(--text); }
  .stat-cell { display: flex; flex-direction: column; gap: 6px; }
  .stat-num { font-family: var(--font-display); font-size: 3rem; font-weight: 800; color: var(--accent-light); line-height: 1; letter-spacing: -.04em; }
  .stat-label { color: var(--text-muted); font-size: .85rem; font-weight: 500; }
  @media (max-width: 768px) { .about-grid { grid-template-columns: 1fr; } .bento-bio { grid-column: 1; grid-row: auto; } }

  #skills { padding: 80px 0 120px; }
  .skills-bento { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; }
  .skill-cell-title { font-family: var(--font-display); font-size: .8rem; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; color: var(--accent-light); margin-bottom: 16px; }
  .skill-tags { display: flex; flex-wrap: wrap; gap: 8px; }
  .skill-tag { background: var(--bg3); border: 1px solid var(--border); border-radius: 100px; padding: 5px 14px; font-size: .8rem; color: var(--text-muted); font-weight: 500; transition: border-color .2s, color .2s; }
  .skill-tag:hover { border-color: var(--accent-light); color: var(--accent-light); }
  @media (max-width: 768px) { .skills-bento { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 480px) { .skills-bento { grid-template-columns: 1fr; } }

  #projects { padding: 80px 0 120px; }
  .projects-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; }
  .project-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 32px; transition: border-color .3s, transform .15s; position: relative; overflow: hidden; }
  .project-card::before { content: ''; position: absolute; inset: 0; border-radius: inherit; background: radial-gradient(600px circle at var(--mx,50%) var(--my,50%), rgba(124,58,237,.08), transparent 60%); pointer-events: none; opacity: 0; transition: opacity .3s; }
  .project-card:hover::before { opacity: 1; }
  .project-card:hover { border-color: var(--accent); }
  .project-idx { font-family: var(--font-display); font-size: .75rem; font-weight: 700; color: var(--accent-light); letter-spacing: .1em; margin-bottom: 12px; display: block; }
  .project-name { font-family: var(--font-display); font-size: 1.25rem; font-weight: 700; color: var(--text); margin-bottom: 12px; }
  .project-desc { color: var(--text-muted); font-size: .9rem; line-height: 1.7; margin-bottom: 20px; }
  .project-techs { display: flex; flex-wrap: wrap; gap: 6px; }
  .project-tech { background: rgba(124,58,237,.1); border: 1px solid rgba(124,58,237,.25); color: var(--accent-light); border-radius: 4px; padding: 3px 10px; font-size: .75rem; font-weight: 600; }
  @media (max-width: 768px) { .projects-grid { grid-template-columns: 1fr; } }

  #experience { padding: 80px 0 120px; }
  .timeline { position: relative; padding-left: 32px; }
  .timeline::before { content: ''; position: absolute; left: 0; top: 0; bottom: 0; width: 1px; background: linear-gradient(to bottom, var(--accent), transparent); }
  .timeline-item { position: relative; margin-bottom: 48px; }
  .timeline-dot { position: absolute; left: -38px; top: 6px; width: 12px; height: 12px; border-radius: 50%; background: var(--accent); box-shadow: 0 0 16px var(--accent-glow); border: 2px solid var(--bg); }
  .timeline-role { font-family: var(--font-display); font-size: 1.15rem; font-weight: 700; color: var(--text); margin-bottom: 4px; }
  .timeline-company { color: var(--accent-light); font-size: .9rem; font-weight: 600; margin-bottom: 4px; }
  .timeline-date { color: var(--text-dim); font-size: .8rem; font-weight: 500; letter-spacing: .06em; margin-bottom: 16px; }
  .timeline-bullets { list-style: none; }
  .timeline-bullets li { color: var(--text-muted); font-size: .9rem; line-height: 1.7; padding-left: 16px; position: relative; margin-bottom: 6px; }
  .timeline-bullets li::before { content: '→'; position: absolute; left: 0; color: var(--accent-light); font-size: .8rem; }

  #education { padding: 80px 0 120px; }
  .edu-certs-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; }
  .edu-section-title { font-family: var(--font-display); font-size: 1rem; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; color: var(--accent-light); margin-bottom: 28px; border-bottom: 1px solid var(--border); padding-bottom: 12px; }
  .edu-item { margin-bottom: 28px; }
  .edu-name { font-weight: 600; color: var(--text); margin-bottom: 4px; }
  .edu-sub { color: var(--text-muted); font-size: .875rem; line-height: 1.5; }
  .edu-meta { color: var(--text-dim); font-size: .8rem; margin-top: 2px; }
  .cert-list { list-style: none; }
  .cert-list li { display: flex; align-items: center; gap: 10px; color: var(--text-muted); font-size: .9rem; padding: 10px 0; border-bottom: 1px solid rgba(255,255,255,.04); }
  .cert-list li .icon { color: var(--accent-light); font-size: .8rem; flex-shrink: 0; }
  @media (max-width: 768px) { .edu-certs-grid { grid-template-columns: 1fr; gap: 40px; } }

  #contact { padding: 120px 0; text-align: center; }
  .closer-inner { background: var(--surface); border: 1px solid var(--border); border-radius: 24px; padding: 80px 48px; position: relative; overflow: hidden; }
  .closer-inner::before { content: ''; position: absolute; inset: -50%; background: radial-gradient(circle at 50% 50%, var(--accent-glow), transparent 70%); pointer-events: none; }
  .closer-title { font-family: var(--font-display); font-size: clamp(2rem, 5vw, 4rem); font-weight: 800; letter-spacing: -.03em; color: var(--text); margin-bottom: 16px; position: relative; }
  .closer-sub { color: var(--text-muted); max-width: 480px; margin: 0 auto 40px; position: relative; }
  .closer-ctas { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; position: relative; }
  .contact-links { display: flex; gap: 24px; justify-content: center; margin-top: 40px; flex-wrap: wrap; position: relative; }
  .contact-link { display: flex; align-items: center; gap: 8px; color: var(--text-muted); text-decoration: none; font-size: .875rem; transition: color .2s; }
  .contact-link .icon { color: var(--accent-light); }
  .contact-link:hover { color: var(--accent-light); }

  footer { text-align: center; padding: 32px; color: var(--text-dim); font-size: .78rem; border-top: 1px solid rgba(255,255,255,.04); }
  footer span { color: var(--accent-light); }

  .reveal { opacity: 0; transform: translateY(32px); transition: opacity .7s, transform .7s; }
  .reveal.visible { opacity: 1; transform: translateY(0); }
  .reveal-delay-1 { transition-delay: .1s; }
  .reveal-delay-2 { transition-delay: .2s; }
  .reveal-delay-3 { transition-delay: .3s; }
  .reveal-delay-4 { transition-delay: .4s; }

  @keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }
  @keyframes fadeIn { to { opacity: 1; } }

  .icon { display: inline-block; width: 1em; height: 1em; vertical-align: -0.125em; fill: currentColor; }
  .mesh { position: absolute; border-radius: 50%; filter: blur(80px); pointer-events: none; opacity: .15; }
</style>
</head>
<body>

<div id="progress-top"></div>
<div id="progress-right"></div>
<div id="cursor-dot"></div>
<div id="cursor-ring"></div>

<nav id="navbar">
  <a class="nav-logo" href="#hero" id="logo-btn">
    PS<span>.</span>
    <div id="theme-label">Electric Violet</div>
  </a>
  <ul class="nav-links">
    <li><a href="#about">About</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#projects">Projects</a></li>
    <li><a href="#experience">Experience</a></li>
    <li><a href="#education">Education</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<!-- ── HERO ── -->
<section id="hero">
  <canvas id="hero-canvas"></canvas>
  <div class="container">
    <div class="hero-inner">
      <div class="hero-content">
        <p class="hero-eyebrow">AI Engineer · GenAI & RAG · Coimbatore, India</p>
        <h1 class="hero-name">
          Preethi<br>
          <span class="accent-word" id="scramble-word">S.</span>
        </h1>
        <p class="hero-tagline">Building AI systems that ground language models in real data — RAG pipelines, semantic search, and vector databases — backed by strong SQL, ETL, and data engineering fundamentals.</p>
        <div class="hero-ctas">
          <a href="https://drive.google.com/file/d/1cBIGSM5l7JdHhY3WlIv7hM8I5VEA9rEZ/view?usp=sharing" target="_blank" class="btn btn-primary magnetic">
            <svg class="icon" viewBox="0 0 512 512"><path d="M288 32c0-17.7-14.3-32-32-32s-32 14.3-32 32V274.7l-73.4-73.4c-12.5-12.5-32.8-12.5-45.3 0s-12.5 32.8 0 45.3l128 128c12.5 12.5 32.8 12.5 45.3 0l128-128c12.5-12.5 12.5-32.8 0-45.3s-32.8-12.5-45.3 0L288 274.7V32zM64 352c-35.3 0-64 28.7-64 64v32c0 35.3 28.7 64 64 64H448c35.3 0 64-28.7 64-64V416c0-35.3-28.7-64-64-64H346.5l-45.3 45.3c-25 25-65.5 25-90.5 0L165.5 352H64zm368 56a24 24 0 1 1 0 48 24 24 0 1 1 0-48z"/></svg> Download Resume
          </a>
          <a href="#projects" class="btn btn-secondary magnetic">
            <svg class="icon" viewBox="0 0 448 512"><path d="M349.4 44.6c5.9-13.7 1.5-29.7-10.6-38.5s-28.6-8-39.9 1.8l-256 224c-10 8.8-13.6 22.9-9 35.3S50.7 288 64 288H175.5L98.6 467.4c-5.9 13.7-1.5 29.7 10.6 38.5s28.6 8 39.9-1.8l256-224c10-8.8 13.6-22.9 9-35.3s-16.3-20.8-29.6-20.8H272.5L349.4 44.6z"/></svg> See My Work
          </a>
        </div>
        <div class="hero-socials">
          <a href="https://github.com/Preethi-Senthil-25" target="_blank" aria-label="GitHub"><svg class="icon" viewBox="0 0 496 512"><path d="M165.9 397.4c0 2-2.3 3.6-5.2 3.6-3.3.3-5.6-1.3-5.6-3.6 0-2 2.3-3.6 5.2-3.6 3-.3 5.6 1.3 5.6 3.6zm-31.1-4.5c-.7 2 1.3 4.3 4.3 4.9 2.6 1 5.6 0 6.2-2s-1.3-4.3-4.3-5.2c-2.6-.7-5.5.3-6.2 2.3zm44.2-1.7c-2.9.7-4.9 2.6-4.6 4.9.3 2 2.9 3.3 5.9 2.6 2.9-.7 4.9-2.6 4.6-4.6-.3-1.9-3-3.2-5.9-2.9zM244.8 8C106.1 8 0 113.3 0 252c0 110.9 69.8 205.8 169.5 239.2 12.8 2.3 17.3-5.6 17.3-12.1 0-6.2-.3-40.4-.3-61.4 0 0-70 15-84.7-29.8 0 0-11.4-29.1-27.8-40.3 0 0-22.9-15.7 1.6-15.4 0 0 24.9 2 38.6 25.8 21.9 38.6 58.6 27.5 72.9 20.9 2.3-16 9-27.5 16.1-34.3-55.1-6.2-110.5-27.4-110.5-122.8 0-27.4 7.6-41 21.4-58.2-2.3-5.7-9.9-29.2 2.3-60.8 18.9-5.7 62.4 25 62.4 25 18.2-5 37.7-7.6 57.1-7.6s38.9 2.6 57.1 7.6c0 0 43.5-31 62.4-25 12.3 31.6 4.6 55.1 2.3 60.8 13.8 17.4 22.1 31 22.1 58.2 0 95.7-58.4 116.6-113.5 122.8 9 7.9 16.7 22.8 16.7 46.2 0 33.5-.3 75.2-.3 83.5 0 6.4 4.6 14.4 17.3 12.1C428.2 457.8 496 362.9 496 252 496 113.3 383.5 8 244.8 8zM97.2 352.9c-1.3 1-1 3.3.7 5.2 1.6 1.6 3.9 2.3 5.2 1 1.3-1 1-3.3-.7-5.2-1.6-1.6-3.9-2.3-5.2-1zm-10.8-8.